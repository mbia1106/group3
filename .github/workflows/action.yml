name: Actions Workflow 

on:
  schedule:
    - cron: '* * * * *'
  push:
    branches:
      - main
      - test
jobs:
  run-shell-command:
    runs-on: self-hosted
    steps: 
      - name: echo a string
        run: echo "Hello World"
        timeout-minutes: 0
      - name: multiline script 
        run: |
        pwd
        ls -a


